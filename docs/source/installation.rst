Installation & Requirements
===========================

Requirements
------------

- Python 3.6 or above

Dependencies
------------

Install with pip:

.. code-block:: bash

   pip install pycryptodome
   pip install windows-curses   # Only on Windows
